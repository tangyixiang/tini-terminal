# AI Terminal 需求与实现方案

> 项目定位：**跨平台 AI SSH Terminal：一个以高品质 Terminal 为核心、内置 AI Agent 的服务器管理客户端。**
> 
> 目标体验：**阿里云 WebSSH 的终端手感 + Claude Code / Cursor 类 Agent 运维能力 + Xshell / Termius 类服务器管理。**

---

## 一、当前实际实现概览 (Implementation Status)

本项目目前已完成 Phase 1、Phase 2 与 Phase 3 的全量核心特性落地，并完成了 Phase 4 中主题定制与流式微型终端控制台的实现。

| 模块类别 | 核心功能点 | 落地状态 | 对应代码与实现载体 |
| :--- | :--- | :--- | :--- |
| **终端引擎** | xterm.js 5.3 + Fit / Search / WebLinks 插件 | [已实现] | `src/components/terminal/XTerminal.tsx` |
| **终端手感** | Cascadia Mono / Menlo 字体栈，1.42 黄金行高，深灰黑低对比背景 | [已实现] | `src/index.css`, `src/types/theme.ts` |
| **本地终端** | 基于 portable-pty 启动系统原生 Shell (zsh/bash/PowerShell) | [已实现] | `src-tauri/src/pty.rs`, `lib.rs` |
| **远程 SSH** | 基于 ssh2 库实现原生连接，支持密码、私钥路径、私钥内容、Passphrase、SSH-Agent | [已实现] | `src-tauri/src/ssh.rs`, `lib.rs` |
| **双向流** | Tauri 2 IPC Channel 双向字节流直灌终端缓冲区，绕开 React 状态分发 | [已实现] | `src/components/terminal/XTerminal.tsx` |
| **多 Tab 会话** | TitleBar 居中活跃会话 + SecondaryTabBar 紧凑工作条，支持同机多开 (#1, #2) | [已实现] | `src/components/terminal/SecondaryTabBar.tsx` |
| **会话池总览** | 会话快速搜索、一键平滑切换、清屏、全选复制、断开重试、批量关闭 | [已实现] | `src/components/terminal/SecondaryTabBar.tsx` |
| **统一侧边栏** | 侧边栏顶部 [ Terminal ] [ Workspace ] 零跳动模式切换，两模式统一宽度与主机树 | [已实现] | `src/components/servers/ServerSidebar.tsx` |
| **服务器管理** | SQLite 本地持久化，支持多主机分组、标签检索、快速连接、平滑激活 | [已实现] | `src-tauri/src/storage.rs`, `src/components/servers/ServerSidebar.tsx` |
| **SFTP 抽屉** | 远程目录树遍历、可点击面包屑导航、路径直接输入编辑 | [已实现] | `src/components/sftp/FileManagerDrawer.tsx`, `src-tauri/src/sftp.rs` |
| **在线编辑** | 远程文件在线读取、实时编辑与原位覆盖保存 | [已实现] | `sftp_read_file`, `sftp_write_file` |
| **文件操作** | 新建文件、新建文件夹、重命名、删除、下载至 Downloads 目录 | [已实现] | `sftp_mkdir`, `sftp_create_file`, `sftp_rename`, `sftp_remove` |
| **文件上传** | 本地文件选择上传，支持访达/资源管理器原生文件拖拽穿透上传 | [已实现] | `sftp_upload_file`, `sftp_upload_bytes`, Tauri 原生 `onDragDropEvent` |
| **AI 模型网关** | 兼容 OpenAI 协议 (DeepSeek, OpenAI, Ollama, vLLM, Custom) | [已实现] | `src-tauri/src/agent.rs`, `src/stores/useSettingsStore.ts` |
| **多配置管理** | 模型多配置独立维护 (新增/编辑/删除/切换激活)，内置连通性与延迟测试 | [已实现] | `src/components/modals/SettingsModal.tsx`, `test_ai_connection` |
| **多 Shell 独立 AI**| AI 智能运维与每个打开的 Shell 1:1 独立绑定，切换 Shell 自动切换对话，历史独立保存 | [已实现] | `src/stores/useAgentStore.ts`, `src/components/agent/AgentPanel.tsx` |
| **流式智能体** | 流式推理响应，支持深度思考链 (Thinking) 实时折叠、计时与单步复制 | [已实现] | `src/components/agent/AgentPanel.tsx`, `runner.ts` |
| **工具闭环** | terminal_exec, file_read, file_write, file_list, system_info | [已实现] | `src/lib/agent/tools.ts` |
| **微型终端** | 为 terminal_exec 配备行内实时 ANSI 彩色微型控制台，支持耗时与弹窗放大 | [已实现] | `src/components/agent/MiniTerminalConsole.tsx` |
| **工作区协同智能体**| 多主机协同 Agent，全屏对话流交互，AI 感知多机并跨服务器调用运维工具 | [方案就绪] | 原型已落地，详见 `prototype.html` |
| **跨机工具调用体系**| host_exec, host_file_read, host_file_write, host_transfer, host_info | [方案就绪] | 原型已落地，架构方案待确认 |
| **权限控制** | 需确认 (ask)、只读 (read_only)、全权 (full) 三档权限模型 | [已实现] | `src/stores/useAgentStore.ts`, 单步授权卡片 |
| **高危拦截** | 静态语法模式拦截 rm -rf /, mkfs, dd, reboot, shutdown, iptables -F 等 | [已实现] | `src-tauri/src/safety.rs` |
| **外观与主题** | 内置 5 套主题 (默认主题, VS Code Dark+, GitHub Dark/Light, One Dark) | [已实现] | `src/types/theme.ts`, TitleBar 切换浮层 |
| **动态排版** | 终端与 Agent 字号 11px~22px 无级微调、双侧栏自由拖拽调整宽度 | [已实现] | `src/App.tsx`, `src/components/layout/TitleBar.tsx` |
| **MCP 协议** | Model Context Protocol 标准扩展工具插件系统 | [待规划] | 计划于后续版本引入 |
| **多 Agent 协作**| 多智能体协作与后台常驻自动化巡检 | [待规划] | 计划于后续版本引入 |

---

## 二、系统架构

```text
+---------------------------------------------------------------------------------+
|                                  AI Terminal                                    |
|                                                                                 |
| 前端表现层 (React 19 + TypeScript + Vite + Tailwind CSS v4)                     |
| - 顶栏与全局动作 (TitleBar: 主题/字号/主机弹窗/SFTP/AI开关)                     |
| - 统一管理侧边栏 (ServerSidebar: [Terminal / Workspace] 模式切换与主机勾选)    |
| - Terminal 模式视口:                                                            |
|   * 二级标签与会话池控制栏 (SecondaryTabBar)                                    |
|   * 高品质终端视口 (XTerminal + xterm.js)                                       |
|   * 1:1 Shell 绑定 AI 运维智能体面板 (AgentPanel + MiniTerminalConsole)         |
| - Workspace 模式视口:                                                           |
|   * 工作区执行看板 (WorkspaceAgentView: 任务流、并发矩阵、节点日志、快捷模版)   |
| - SFTP 远程文件管理抽屉 (FileManagerDrawer)                                     |
| - 全局响应式状态 (Zustand: Server, Terminal, Agent(多Session), Workspace, Set)  |
|                                                                                 |
| 桌面通信层 (Tauri 2 IPC + Channel 双向流式管道)                                 |
|                                                                                 |
| 核心后端层 (Rust 1.98.1 + Tokio 异步运行时)                                    |
| - 本地 PTY 驱动器 (portable-pty: zsh / bash / PowerShell)                       |
| - SSH2 协议驱动 (ssh2: 交互终端会话与同步管道)                                  |
| - 会话上下文管理 (session: Shell 会话跟踪与映射)                                |
| - 工作区引擎 (workspace: 工作区定义与主机拓扑管理)                             |
| - 异步多机任务调度器 (task: 并发 SSH 执行、输出归集与审计落库)                  |
| - SFTP 传输控制器 (sftp: 文件遍历、读写、流式上传下载)                          |
| - 嵌入式存储引擎 (rusqlite / SQLite: servers, settings, workspaces, tasks)      |
| - 高危指令拦截器 (safety: 正则静态匹配与审计风险定级)                           |
| - AI 协议代理 (agent: HTTP 流式调用与请求中断机制)                              |
+---------------------------------------------------------------------------------+
                                  |
                                  +---> 目标 Linux / Unix / 本地环境 (单机 / 集群)
```

---

## 三、核心模块实现细节

### 1. 终端手感与渲染核心 (Terminal Core)

* **渲染器与扩展插件**：采用 xterm.js 核心引擎，搭载 `@xterm/addon-fit` 自动计算行列网格，`@xterm/addon-search` 支持选区检索，`@xterm/addon-web-links` 自动识别终端中的 URL 链接。
* **严格等宽字体**：
  * macOS: `Menlo, Monaco, 'PingFang SC', monospace`
  * Windows: `'Cascadia Mono', Consolas, 'Microsoft YaHei', monospace`
  * 字符间距设为 0，彻底消除制表符表格、ASCII 字符图与 vim/tmux 分屏界面的对齐错位。
* **排版规格**：
  * 默认字号 14px，行高固定为 1.42（即 20px 真实行高），字符纵向间距适中紧凑。
  * 光标默认采用细竖条（Bar）配合平滑闪烁（Blink），宽度为 2px。
  * 滚动回滚缓冲区设置为 10,000 行。
* **极低输入延迟机制**：
  * 终端键盘输入监听 `term.onData`，输入数据通过批量缓冲队列直投至 Rust 后端命令 `send_terminal_input`，绕开 React 虚拟 DOM 状态更新。
  * 后端根据当前终端是本地还是 SSH，直接调用底层 `pty.write_data` 或 `ssh.write_data`。
* **尺寸自适应同步**：
  * 使用 `ResizeObserver` 监听 DOM 容器尺寸变更，容器有效时触发 `fitAddon.fit()`。
  * 随后立即向后端发起 `resize_terminal` 指令，同步远端 PTY 的 cols 与 rows，保证 top、htop、vim、docker 窗口渲染始终与视图一致。

### 2. 多 Tab 会话与双层导航体系 (Multi-Tab & Session Architecture)

* **双层标签架构**：
  * **第一层（TitleBar）**：居中展示当前激活的终端会话信息胶囊（如 `root@prod-01 (192.168.1.100)`），附带连接状态指示点（绿色常亮表示已连接，黄色闪烁表示正在建立连接）。
  * **第二层（SecondaryTabBar）**：紧贴终端上方的高效二级工作条（高度 34px）：
    * 横向平滑滚动标签列表，支持鼠标滚轮左右滑动。
    * 每个标签包含连接状态指示点、终端类型图标（远程主机为 Server 图标，本地终端为 Terminal 图标）、会话名称。
    * **同机多开序号标识**：自动检测同一远程服务器的多开会话，显示 `#1`、`#2` 等序号徽标。
    * 快捷关闭按钮与鼠标中键单击关闭支持。
    * 快捷添加本地终端按钮（`+`）。
* **会话工具与池管理**：
  * 二级工作条右侧集成快捷操作：
    * **重试连接 (Retry)**：针对连接断开的会话，一键重连并刷新屏幕。
    * **清屏 (Clear)**：一键清空当前终端缓冲区。
    * **复制全部 (Copy All)**：一键提取终端全部选区与输出文本至系统剪贴板。
    * **会话总览下拉面板 (Layers)**：点击展开所有活跃会话列表，支持输入关键字实时检索，并支持批量操作（“关闭其他会话”、“关闭全部会话”）。
* **统一侧边栏与主模式切换 (Unified Sidebar & Mode Switcher)**：
  * 侧边栏顶部常驻 `[ Terminal ] [ Workspace ]` 切换器，两个按钮采用等宽、居中排版，点击切换时保持几何尺寸零抖动，仅改变高亮背景色与文字状态。
  * 侧边栏宽度统一维护在 `useSettingsStore`，跨模式拖拽平滑保持。
  * 统一共享底层主机分组数据与搜索过滤能力，模式切换无需重建组件树。

### 3. 服务器连接与本地存储 (Server Management & SQLite)

* **本地轻量级数据库持久化**：
  * 采用嵌入式 SQLite（`rusqlite`），文件保存在应用数据目录的 `ai_terminal.db`。
  * `servers` 表结构：
    * `id`: TEXT 主键
    * `name`: TEXT 连接显示名称
    * `host`: TEXT 主机地址（IP 或域名）
    * `port`: INTEGER 端口号（默认 22）
    * `username`: TEXT 登录用户名
    * `auth_type`: TEXT 认证模式（`key` / `password` / `agent`）
    * `credential`: TEXT 登录凭证（密码明文，或私钥绝对路径，或私钥文本内容）
    * `passphrase`: TEXT 私钥保护密码（可选）
    * `group_name`: TEXT 所属分组（如生产集群、开发测试）
    * `tags`: TEXT 分类标签逗号分隔
    * `created_at`: INTEGER 创建时间戳
* **认证方式全支持**：
  * 密码认证：直接使用提供的密码完成握手。
  * 私钥认证：自动兼容文件路径（如 `~/.ssh/id_ed25519`，自动展开波浪号路径）与直接粘贴的 PEM/OpenSSH 格式私钥文本。
  * 本地 SSH Agent 认证：通过系统的 SSH Agent 转发认证。
* **平滑连接体验**：
  * 点击左侧主机列表时，如果该主机已存在打开的会话标签，自动平滑切换至该标签并重聚焦，杜绝误触多次发起并行连接。
  * 提供单独的“新建终端标签”按钮允许显式多开。

### 4. SFTP 远程文件管理系统 (SFTP Drawer)

* **抽屉式工作空间**：
  * 采用右侧滑出抽屉布局（宽度 580px），与终端工作区并行互不遮挡。
  * 自动绑定当前活跃终端会话对应的 SSH 客户端连接。
* **文件导航与管理**：
  * 可点击的面包屑路径切分（`/` > `var` > `log`），支持任意层级快速回退。
  * 单击路径区域可切换为输入框模式，直接键入远程绝对路径跳转。
  * 支持返回上一级目录（`ArrowUp`）。
  * 列表展示文件名称、大小（自动换算 B/KB/MB）、权限位、修改时间。
* **在线查看与编辑**：
  * 单击文本文件立即进入在线编辑模式。
  * 支持修改配置内容并点击“保存覆盖”，后端通过 `sftp_write_file` 实时写回服务器。
* **文件与目录全生命周期**：
  * 新建文件（`sftp_create_file`）与新建文件夹（`sftp_mkdir`）。
  * 重命名（`sftp_rename`）与删除（`sftp_remove`，支持目录递归清理）。
  * 文件一键下载到本机的系统的 Downloads 目录。
* **拖拽与上传穿透**：
  * 支持文件选择器批量上传。
  * 支持将外部操作系统访达/文件资源管理器中的文件、图片直接拖拽释放至抽屉面板，后端通过 Tauri 2 的 `onDragDropEvent` 捕获绝对物理路径并无缝传输至当前远程目录。

### 5. AI Agent 智能运维系统 (AI Agent Runtime)

* **统一协议与模型兼容**：
  * 基于标准 OpenAI Chat Completions 协议封装，预设对 DeepSeek、OpenAI、Ollama、vLLM 及自定义接口的支持。
  * 支持多配置管理体系，用户可在设置面板中自由添加配置、切换激活配置并一键发起连接连通性与 HTTP 延迟测速。
* **流式交互与思维链呈现**：
  * Rust 后端通过 `stream_ai_chat` 流式推送 Chunk，前端实时增量解析。
  * 深度思考链（Thinking / Reasoner）展示：针对推理型大模型（如 deepseek-reasoner），独立呈现可折叠的推理过程卡片，包含实时动态耗时秒数计时器与一键复制功能。
  * AI 最终诊断结果采用 Markdown 结构化呈现，内置代码高亮、表格排版与复制回复按键。
* **闭环运维工具系统 (Tool Calling)**：
  * `terminal_exec`：在目标主机或本地 Shell 上执行命令，捕获 exit_code、stdout、stderr 与执行耗时。
  * `file_read`：读取远程指定绝对路径的文本文件。
  * `file_write`：在服务器上写入或覆写文件（支持自动创建多级父目录），用于落盘部署脚本与配置文件。
  * `file_list`：列出指定目录下的文件与目录元数据。
  * `system_info`：快速采集目标服务器的系统版本、运行时间与资源使用状态。
* **行内实时彩色微型终端控制台 (MiniTerminalConsole)**：
  * 专为 `terminal_exec` 工具调用设计。
  * 解析 ANSI Escape Code 颜色（红色、绿色、黄色、蓝色、洋红、青色等高对比色）。
  * 工具执行时以微型窗口展示命令与输出，显示返回状态码与耗时。
  * 支持点击弹出全屏大弹窗（Portal 模式）进行深度复盘与长篇日志检索，支持一键无样式纯文本复制。
* **安全权限控制模式**：
  * **需确认 (ask，默认)**：针对工具调用生成单步授权卡片，显示调用参数与安全提示，由用户决定 [允许执行] 或 [拒绝]。
  * **只读模式 (read_only)**：仅允许只读查询类操作，严格阻断写操作。
  * **全权模式 (full)**：Agent 自主执行全量运维与诊断闭环。
* **Shell 1:1 独立会话体系 (Per-Shell Isolated Agent Sessions)**：
  * **按 Tab 隔离的会话状态模型**：前端 `useAgentStore` 以 `tabId` 为主键维护 `sessions: Record<string, SessionAgentState>`。每个 Shell 拥有完全隔离的独立状态：
    * `messages`：专属对话流，包含用户输入、推理思考过程、工具调用卡片与 AI 结构化回复。
    * `isThinking`：专属思考中状态与实时毫秒计时器。
    * `pendingToolCall`：专属待确认工具单步授权卡片。
    * `approvalResolver`：专属人工审批 Promise 回调决议器。
  * **无感平滑切换与对话保留**：用户在多 Tab / 多 Shell 间切换时，`AgentPanel` 响应式重新绑定当前 `activeTabId`，界面瞬间刷新为对应 Shell 的历史对话与审批流，完整保留所有历史痕迹。
  * **动态生命周期与内存释放**：新建 Shell 时自动初始化纯净空白的会话上下文；关闭 Tab 时自动触发 `removeSession(tabId)` 清理其会话内存，彻底杜绝状态残留与悬挂闭包。
  * **绑定提示与执行定向**：Agent 面板顶部直观展示当前绑定的终端名称胶囊（如 `prod-01`, `本地 Shell`）；执行 `terminal_exec` 时严格定向至该 Tab 对应的底层会话句柄。
* **高危指令拦截器 (Safety Guard)**：
  * 独立 Rust 模块 `safety.rs`，采用静态正则深度扫描拦截破坏性命令：
    * `rm -rf /`, `rm -rf /*`, `rm -rf ..` 等强力递归删除指令
    * `mkfs`, `mkfs.ext4` 等磁盘格式化指令
    * `dd if=... of=/dev/...` 裸磁盘覆写
    * `shutdown`, `reboot`, `poweroff`, `init 0` 等系统停机指令
    * `iptables -F`, `iptables --flush` 防火墙清空
    * `DROP DATABASE`, `DROP SCHEMA` 数据库删除
    * Fork 炸弹代码
  * 针对高危指令进行红色警告弹窗拦截并强制用户确认。

### 6. 外观与主题系统 (Theme & Appearance)

* **内置 5 套精调主题**：
  1. **默认主题 (aliyun)**：深灰黑低对比背景（#121212 / #171717），翡翠绿强调色（#23d18b），还原阿里云 WebSSH 的高耐看手感。
  2. **VS Code Dark+**：经典 VS Code 编辑器暗色风格（#1e1e1e / #252526），蓝色高亮（#0078d4）。
  3. **GitHub Dark**：GitHub 官方深色质感（#0d1117 / #161b22），天蓝强调色（#2f81f7）。
  4. **GitHub Light (浅色)**：明亮清爽的白底灰色调（#ffffff / #f6f8fa），适合日间办公环境。
  5. **One Dark Pro**：经典 Atom One Dark 配色方案（#282c34 / #2c313a），青蓝色强调（#61afef）。
* **系统原生联动**：
  * 切换浅色/暗色主题时，自动调用原生窗口接口 `set_window_theme` 同步 macOS 窗口外观。
* **字号即时动态缩放**：
  * TitleBar 提供 `A-` / `A+` 快捷按键，Settings 弹窗内提供 11px~22px 滑块。
  * 调整时终端自动调用 `fitAddon.fit()` 并刷新重绘，Agent 面板字号与间距同步等比例缩放。

### 7. 工作区协同智能体系统 (Workspace Multi-Host Autonomous Agent)

* **核心定位与交互变革**：
  * **非同构命令广播，而是多机协同智能调度**：工作区模式并非在多台服务器上机械并发执行相同 Shell 指令，而是让 AI 获得可见集群中多台服务器的上下文，根据用户的最终目标自主决策，按需访问不同的服务器完成跨节点组合任务。
  * **全屏纯净对话工作区 (Chat-First Interface)**：进入 Workspace 后呈现为单一、沉浸式对话窗口。用户输入高阶运维目标，AI 自主规划执行路径、分步跨机调用工具并输出最终闭环报告。
* **可见服务器集群上下文 (Visible Server Scope)**：
  * 左侧侧边栏保留主机树状分组与复选框，用于动态定义当前 AI 的“可见服务器范围”。
  * 勾选状态在对话顶栏实时以主机胶囊徽章呈现（如 `AI 可见主机: [prod-01] [prod-02]`）。
  * AI 的系统提示词（System Prompt）动态注入所有可见主机的元数据（主机名、IP、操作系统、SSH 连接凭证标识）。
* **跨机运维工具集体系 (Multi-Host Tool Calling)**：
  * `host_exec(host_id, command)`：在指定目标主机上执行 Shell 命令，捕获其专有 stdout、stderr、返回码与执行耗时。
  * `host_file_read(host_id, path)`：读取指定远程主机上的配置文件或排障日志。
  * `host_file_write(host_id, path, content)`：向指定远程主机写入或覆盖配置文件。
  * `host_transfer(src_host_id, src_path, dst_host_id, dst_path)`：跨服务器安全复制/同步文件或静态资源包。
  * `host_info(host_id)`：快速感知指定主机的负载、内存与关键服务存活状态。
* **携带主机标识的结构化工具流与审批**：
  * 对话流中的每个工具调用卡片均醒目标注目标主机名徽章（如 `[prod-01]`、`[prod-02]`），清晰展示每一步骤访问的具体服务器。
  * 工具调用内嵌微型 ANSI 彩色控制台，记录单机返回状态与执行耗时。
  * **单机安全审批拦截**：高危写操作或破坏性指令严格触发审批卡片，卡片明确标识影响的具体主机（如 `[prod-02] 待核准高危操作: systemctl start nginx`），避免全局盲目执行。
* **智能总结与闭环报告**：
  * AI 在串联访问完各个主机后，对各节点执行结果进行综合归纳，输出结构化 Markdown 闭环总结。

---

## 四、界面整体布局

### 1. Terminal 模式视图 (单机交互式 + 1:1 Shell 独立 AI 运维)

```text
+-----------------------------------------------------------------------------------------+
| [macOS] [折叠] (图标) TINI TERMINAL v1.0       [活跃会话胶囊]        [A-/A+] [主题] [加主机] [SFTP] [AI] [设置] |
+-----------------------------------------------------------------------------------------+
| [● Shell 1 /etc/nginx] [● Shell 2 /var/log] [● Shell 3 ~] [+]        [重试] [清屏] [复制] [会话池(3) v] |
+------------------+---------------------------------------------------+------------------+
| [Term] [Worksp]  |  root@prod-01:/etc/nginx# nginx -t                | AI 智能运维 [Shell 1] |
| ---------------- |  nginx: configuration file /etc/nginx/nginx.conf  | ---------------- |
| 服务器列表 (Hosts) |  test is successful                               | 用户提问与时间戳 |
| 搜索主机/IP...   |                                                   | ---------------- |
| [本地环境]       |  root@prod-01:/etc/nginx# systemctl reload nginx  | 思考推理链 (1.2s)|
| > 本地 Shell     |                                                   | ---------------- |
|                  |  root@prod-01:/etc/nginx# uptime                  | terminal_exec:   |
| [生产集群] (2)   |   17:42:15 up 12 days, load average: 0.18         | $ nginx -t       |
| > prod-01 (活动) |                                                   | [微型彩色终端输出]|
| > prod-02        |  root@prod-01:/etc/nginx# |                        | ---------------- |
|                  |                                                   | 待核准操作卡片   |
| [开发测试] (1)   |                                                   | [拒绝] [核准执行]|
| > dev-01         |                                                   | ---------------- |
|                  |                                                   | [输入框与发送键] |
+------------------+---------------------------------------------------+------------------+
| 已连接: prod-01 (192.168.1.101:22)               延迟: 18ms               编码: UTF-8    |
+-----------------------------------------------------------------------------------------+
```

### 2. Workspace Agent 模式视图 (多机协同对话工作区)

```text
+-----------------------------------------------------------------------------------------+
| [macOS] [折叠] (图标) TINI TERMINAL v1.0   工作区: 生产集群协同   [A-/A+] [主题] [加主机] [设置]    |
+-----------------------------------------------------------------------------------------+
| [Term] [Worksp]  | 运维工作区 | AI 可见主机: [● prod-01] [● prod-02]    [只读/确认/自动] [清空] |
| ---------------- | --------------------------------------------------------------------- |
| 运维工作区       | 用户: 检查 prod-01 的 Nginx 配置，复制到 prod-02，测试语法通过后启动服务 |
| [x] 全选目标主机 | --------------------------------------------------------------------- |
| 搜索主机/IP...   | [AI 跨机协同推理链]                                                   |
| ---------------- | - 步骤 1: 访问 prod-01 导出 /etc/nginx/nginx.conf                     |
| [生产集群] (2)   | - 步骤 2: 访问 prod-02 同步配置并执行自检                             |
| [x] prod-01      | - 步骤 3: 在 prod-02 申请权限并启动服务                               |
| [x] prod-02      | --------------------------------------------------------------------- |
|                  | [prod-01] host_exec: nginx -t && cat /etc/nginx/nginx.conf [退出码 0] |
| [未分组] (1)     | [prod-02] host_file_write: /etc/nginx/nginx.conf           [完成 18ms]|
| [ ] dev-01       | [prod-02] host_exec: nginx -t                              [退出码 0] |
|                  | --------------------------------------------------------------------- |
|                  | [prod-02] 待核准高危操作: systemctl start nginx                       |
|                  | 目标主机: prod-02 (192.168.1.102)                       [拒绝] [核准执行]|
|                  | --------------------------------------------------------------------- |
|                  | AI 总结: 已从 prod-01 成功提取配置并同步至 prod-02，服务启动就绪。    |
|                  | --------------------------------------------------------------------- |
|                  | 快捷诉求: [跨机配置对比] [集群磁盘体检] [日志联动处置]                |
|                  | [输入运维诉求，AI 可直接访问勾选的主机协同完成任务...]            [发送] |
+------------------+----------------------------------------------------------------------+
| 统一调度引擎: 就绪                               可见主机: 2 台           编码: UTF-8    |
+-----------------------------------------------------------------------------------------+
```

---

## 五、后续规划与演进 (Roadmap)

1. **MCP (Model Context Protocol) 插件协议支持**：
   * 将 Agent 工具层抽象为兼容 Anthropic MCP 协议的客户端，支持用户挂载外部 MCP Server（如 Git、MySQL、Kubernetes 运维插件）。
2. **多 Agent 协同体系**：
   * 引入子 Agent 派发机制，支持“排障诊断 Agent”、“配置生成 Agent”、“安全审计 Agent”分工协作。
3. **服务器监控大盘与后台巡检**：
   * 后台异步轻量轮询服务器 CPU、内存、磁盘与网络指标。
   * 支持指标异动时主动在侧边栏向用户发送运维报警。
4. **多跳 JumpHost (ProxyJump) 链路支持**：
   * 在主机连接配置中扩充 Bastion / JumpHost 配置，支持经由跳板机穿透内网。
